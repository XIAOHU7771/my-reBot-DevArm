# 本工程如何用 AI 开发（提示词说明）

> **读者**：想用 AI 写代码、但不要 vibe coding（自己糊里糊涂点同意）的新手。  
> **目标**：你始终主导——定范围、定接口、定验收；AI 负责在边界内实现。  
> **配套文档**：  
> - [`新手入门讲解.md`](新手入门讲解.md) — 项目在干什么、B/B2 知识点  
> - [`视触融合任务说明.md`](视触融合任务说明.md) — 视触融合要做什么、架构与里程碑  
> - [`README.md`](README.md) — 安装与已有命令  

---

## 0. 先分清：AI coding vs vibe coding

| | **AI coding（推荐）** | **vibe coding（避免）** |
|--|----------------------|-------------------------|
| 谁定架构 | 你（对照说明文档） | AI 随意发挥 |
| 一次改多少 | 一个里程碑 / 一个模块 | 「全都做完」 |
| 你是否理解 | 能讲清数据流与坐标系 | 只会说「能跑就行」 |
| 出 bug 时 | 你知道该查哪一层 | 让 AI 再盲改三轮 |
| 提示词 | 有范围、接口、验收、禁止 | 「帮我优化一下」 |

**口诀**：范围要窄、接口要清、验收要写、禁止要狠。

---

## 1. 本仓库分层（发提示词前先想清楚改哪一层）

```text
┌─────────────────────────────────────────┐
│ 任务脚本层  task_B / task_B2 / (将来 task_VTG)
│ 状态机、Demo、参数开关                      │
└──────────────────┬──────────────────────┘
                   │ 应尽量调用，少复制
┌──────────────────▼──────────────────────┐
│ 工具层  task_B2/utils/
│ robot.py | gripper.py | ik.py | scene.py   │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│ 仿真与模型  PyBullet + urdf/               │
└─────────────────────────────────────────┘
```

| 你想改的事 | 优先动哪里 | 提示词里怎么写 |
|------------|------------|----------------|
| 点到点运动教学 | `task_B/` | 不要动 B2 力控 |
| 力控/夹爪/IK 行为 | `task_B2/utils/` 或力控脚本 | 说明是否允许改 utils |
| 视触融合新功能 | **只准** `task_VTG/`（待建） | 禁止重构整个 B2 |
| 文档 | `*.md` | 明确「只改文档」 |

**原则**：新任务（视触）= 新目录编排 + **复用** B2；不要把 B2 推倒重写。

---

## 2. 和 AI 协作的标准流程（每次都走）

```text
① 你打开说明文档，选定里程碑 Mx / Step
② 发「设计提示词」→ AI 只出文件列表 + 函数签名 + 数据流
③ 你审查：坐标系对不对？是否越界？验收够不够？
④ 你回复：「确认」或「改设计：……」
⑤ 发「实现提示词」→ AI 写代码
⑥ 你按验收命令跑；失败则发「调试提示词」（带日志）
⑦ 你能用三句话讲清本次改动 → 再开下一个里程碑
```

**不要跳过 ②③**：批设计比批代码便宜。

---

## 3. 万能提示词结构（6 块）

每条提示词尽量包含：

1. **目标**：这一步交付什么  
2. **范围**：允许/禁止改哪些路径  
3. **复用**：必须用的现有模块  
4. **接口**：输入、输出、坐标系  
5. **验收**：命令 + 期望现象  
6. **禁止**：明确不要做什么  

---

## 4. 可复制模板

### 4.1 设计阶段（先不写完整代码）

```text
你是本仓库协作程序员。请严格对照：
- 《视触融合任务说明.md》
- 《新手入门讲解.md》（概念）
当前只做：【里程碑 Mx / 模块名】。

不要写完整业务实现。请只输出：
1) 将创建/修改的文件列表（路径）
2) 每个公开函数/类的签名
3) 数据流（从哪到哪，标注坐标系 camera/base/ee）
4) 失败时怎么表现
5) 我的手动验收步骤（2～3 条）

约束：
- 新功能放在 task_VTG/（若目录不存在则规划创建）
- 力控/夹爪/IK 优先复用 task_B2，说明准备 import 什么
- 禁止 YOLO/ROS/VLA/大重构 B2
等我回复「确认」后再编码。
```

### 4.2 实现阶段（你确认设计之后）

```text
按你上一条已确认的设计实现【Mx】。

硬约束：
1. 不扩大范围；不修改设计里没写的文件（除非我追加允许）
2. 力控必须复用 task_B2（在回复里写明 import 路径与函数名）
3. 检测优先颜色阈值 + PyBullet RGB-D；不要深度学习
4. Eye-to-Hand 顶视优先
5. 关键步骤加简短中文注释（为什么，不是复述代码）

完成后请给出：
- 改动文件列表
- 运行命令（含 --direct 烟测若适用）
- 验收失败时我该看哪些打印/文件
```

### 4.3 调试阶段（带着证据）

```text
不要重写模块。根据以下失败现象定位【Mx】：

【我运行的命令】
...

【完整报错或关键日志】
（粘贴）

【我期望】
...

【我观察到】
...

请：
1) 先给出最可能的 1～3 个原因（对照是视觉、手眼、IK 还是力控层）
2) 只做最小修改
3) 说明如何验证已修好
禁止：顺便重构、换技术栈、删除 B2 功能。
```

### 4.4 解释阶段（确保你懂）

```text
不要写新功能。用小白能懂的话解释你刚才的改动：
1. 数据现在在哪个坐标系？
2. 哪个函数完成 camera→base？方向会不会反？
3. 力控是调用 B2 还是重写的？
4. 检测失败 / IK 失败 / 力一直为 0 时程序怎么走？
5. 若只保留本里程碑，哪些新文件是核心、哪些可删？
```

### 4.5 文档-only

```text
只更新文档，不改 Python。
根据当前仓库真实结构，更新【README 或 task_VTG/README】中的：运行命令、架构图、与 B2 的复用关系。
禁止编造不存在的脚本名。
```

---

## 5. 按任务类型的专用提示词

### 5.1 维护 / 小修现有 B2（力控、闪烁、IK）

```text
问题：【一句话现象】
相关文件：task_B2/……
约束：最小改动；保持 --force-control / --no-force-control 行为语义；
改完用：python task_B2/2.adaptive_force_control_grasp.py --direct --no-show --force-control
说明根因与验证方式。不要开始做视觉。
```

### 5.2 视触融合 — 按里程碑（摘自说明文档）

把下面整段发给 AI，只替换 `Mx` 描述。

**M1 相机存图**

```text
按《视触融合任务说明.md》里程碑 M1。
目标：Eye-to-Hand 顶视 RGB-D，保存 rgb 与 depth 可视化。
范围：仅 task_VTG/vision/camera.py + 最小脚本；可只读复用 B2 load_robot。
禁止：检测、抓取、力控、改 B2 逻辑。
先设计签名，我确认后再写代码。
验收：运行后有图片文件，终端打印分辨率。
```

**M2 定位到基座系**

```text
按说明文档 M2 / Step V2–V3。
目标：红/黄颜色检测 + 深度 → (x,y,z)_base，并与 getBasePosition 比误差。
范围：detect.py、handeye.py、测试脚本。
约束：手眼用仿真已知相机位姿；写清 T_base_cam 方向；打印 err_cm。
禁止：抓取流水线、深度学习。
先设计后实现。
```

**M3 无视觉打通抓放**

```text
按说明文档 M3。
目标：配置文件固定坐标，状态机跑通：预抓取→下探→接触→B2力控→抬升→分拣放置。
范围：pipeline / motion / grasp bridge / place_zones / config。
力控必须 bridge 调用 B2；若需抽取函数先提案。
禁止：OpenCV 检测、重跟踪。
验收：--direct 下两物体进两区 + 状态日志。
```

**M4 视觉接入**

```text
按说明文档 M4。
目标：用视觉坐标替换固定坐标；运行时不手改物体位姿。
范围：只改目标来源与 vision 调用，保持 M3 力控/放置。
验收：随机摆放后一次运行完成分拣；失败要有状态与原因。
```

**M5 Demo 标注**

```text
按交付「画面一」。
目标：RGB 画框并叠加基座系坐标文字，便于录屏。
范围：可视化 + --annotate；尽量不动力控参数。
```

**M6 重跟踪（进阶）**

```text
按说明文档模块3 / M6。
目标：下探中物体 XY 移动超阈值则 retrack 并更新目标。
范围：tracking/ + pipeline 钩子。
验收：强制挪动后日志含 retrack；允许变慢但应尝试再抓。
```

### 5.3 一次会话的「开工第一句」（推荐今天就用）

```text
请严格按仓库《视触融合任务说明.md》与《本工程如何用AI开发（提示词说明）.md》工作。
当前只做里程碑 M1：顶视 RGB-D 存图。
先输出设计（文件列表、签名、验收），不要直接写完整代码。
等我回复「确认」后再实现。
```

---

## 6. 审查清单（AI 交代码后你勾选）

- [ ] 改动是否仍在约定目录内？  
- [ ] 是否出现未申请的「大重构」？  
- [ ] 力控是否真的 import 了 B2，而不是复制一套 PID？  
- [ ] 所有 3D 点是否标明坐标系？  
- [ ] 失败路径是否有日志（检测空、IK 失败、力为 0）？  
- [ ] 我能否不看代码讲出：输入→处理→输出？  
- [ ] README/注释是否提到如何运行本步？  

有一项否 → 打回，用 **4.3 / 4.4** 模板追问。

---

## 7. 坏提示词 vs 好提示词

| 坏（vibe） | 好（AI coding） |
|------------|-----------------|
| 把视触融合全做完 | 只做 M1 相机存图，先出设计 |
| 按最佳实践随便设计架构 | 按《视触融合任务说明》第 2 节目录实现 |
| 顺便重构 B2 | 禁止改 B2 力控逻辑，只允许 import |
| 用 YOLO+SAM+ROS2 | 颜色阈值 + PyBullet RGB-D |
| 不行再试试 | 粘贴命令+日志，要求最小修复 |
| 优化一下代码 | 指出：闪烁 / 窗口跳变 / 力卡在 23N 等具体现象 |

---

## 8. 多轮对话怎么控场

| 轮次 | 你说什么 | AI 应交付 |
|------|----------|-----------|
| 1 | 设计 Mx | 文件+签名+数据流+验收 |
| 2 | 「确认」或「改：……」 | 无代码或改设计 |
| 3 | 实现 | 代码+运行命令 |
| 4 | 跑挂了+日志 | 最小修复 |
| 5 | 解释改动 | 小白级说明 |
| 6 | 「开始 M(x+1)，同样流程」 | 新一轮设计 |

**控场句**（随时可用）：

```text
停止扩需求。回到《视触融合任务说明.md》里程碑 Mx，只完成验收列表中的内容。
```

```text
你越界了：修改了【文件】。请撤回该部分，或说明为何必须改并等我批准。
```

---

## 9. 针对本工程的「复用白名单」（写进提示词）

鼓励 AI **优先使用**：

| 能力 | 位置 |
|------|------|
| 加载臂 | `task_B2/utils/robot.py` → `load_robot` |
| 开合/力 | `task_B2/utils/gripper.py` |
| IK | `task_B2/utils/ik.py` |
| 力控抓取流程 | `task_B2/2.adaptive_force_control_grasp.py`（bridge 调用） |
| 滑移（可选） | `task_B2/3.slip_compensation_test.py` |
| 点到点直觉 | `task_B/rebot_sim.py`（教学，不必强行 import） |

新视觉/流水线代码放 **`task_VTG/`**，不要塞进 `1./2./3.` 脚本里搅成一锅粥。

---

## 10. 安全与质量要求（可粘贴到每次系统提示）

```text
本仓库约束（长期有效）：
1. 不编造不存在的文件路径；不确定就先搜仓库再改。
2. 不 force push、不改 git config、未经要求不提交。
3. 仿真默认可提供 --direct --no-show 烟测方式。
4. GUI 相关：注意 IK 时 reset 导致瞬闪；改渲染开关需说明理由。
5. 中文回复关键设计决策；代码注释讲「为什么」。
6. 一次 PR/一次对话只服务一个里程碑。
```

---

## 11. 文档索引（你迷茫时打开哪个）

| 我的问题 | 打开 |
|----------|------|
| 臂 / 力控基本概念听不懂 | `新手入门讲解.md`（含附录 A 零线代） |
| 视触融合要做成什么样 | `视触融合任务说明.md` |
| **怎么跟 AI 说话、怎么控场** | **本文** |
| 怎么安装、怎么跑已有 Demo | `README.md` |

---

## 12. 最小实践（今天 30 分钟）

1. 读本文第 0、2、4 节。  
2. 复制 **5.3 开工第一句** 发给 AI。  
3. 只看它的「设计」，用第 6 节清单挑刺。  
4. 满意再回「确认」。  
5. 跑通验收后，自己写三句话总结——写不出来就还没主导成功。

---

**一句话**：  
AI 是你的手，**说明文档是图纸，提示词是施工单**。没有图纸和施工单的「帮我做完」，就是 vibe coding。
