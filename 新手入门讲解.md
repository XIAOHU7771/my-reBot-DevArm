# reBot-DevArm 新手入门讲解

> 面向机械臂领域小白：用本仓库当例子，讲清楚**项目在干什么、怎么跑、各功能怎么实现、涉及哪些知识点**。  
> 读完后你应能回答：仿真臂如何从“目标坐标”变成“关节转动”，以及力控抓取大致在控什么。

---

## 0. 先建立直觉：这个项目在干什么？

把机械臂想成一只**被电脑控制的手臂**：

| 现实世界 | 本项目里对应什么 |
|----------|------------------|
| 真实铁臂 + 电机 | **URDF 三维模型** + PyBullet 里的“虚拟电机” |
| 人手眼协调去拿杯子 | **逆运动学（IK）**：知道手要到哪，反算肘/腕怎么弯 |
| 手指捏得轻重 | **力/触觉反馈**：夹爪碰到物体时测“正压力” |
| 捏鸡蛋要轻、捏铁块要紧 | **自适应力控**：根据软硬选安全抓紧力 |
| 提起来后东西要往下滑 | **滑移对冲**：发现在滑就再加一点力 |

本仓库分两块任务：

1. **任务 B（`task_B/`）**：只会“把手伸到指定空间点”——学 **运动学 + 插值运动**。  
2. **任务 B2（`task_B2/`）**：会“抓东西、测力、自适应抓紧、防滑落”——学 **接触力 + 力闭环控制**。

你不需要先会推导雅可比矩阵；先把**数据流**搞懂即可。

> 若觉得「坐标变换 / FK·IK / 线代」劝退：直接跳到文末 **[附录 A：零线代版——坐标与 IK](#附录-a零线代版坐标与-ik直觉优先)**，用积木和本仓库画面建立直觉即可。

---

## 1. 项目架构（文件夹地图）

```text
my-reBot-DevArm/
├── task_B/                 # 入门：点到点运动
│   ├── find_ee.py          # 查「哪个 link 是手」
│   └── rebot_sim.py        # 输入 (x,y,z) → IK → 平滑动过去
│
├── task_B2/                # 进阶：力传感 / 力控 / 滑移
│   ├── 1.Force_Sensor_Simulation.py   # ① 测正压力并画曲线
│   ├── 2.adaptive_force_control_grasp.py  # ② 铁块+海绵自适应抓取
│   ├── 3.slip_compensation_test.py    # ③ 抬起后向下拽，测防滑
│   ├── utils/
│   │   ├── robot.py        # 加载带夹爪的 URDF，找关节编号
│   │   ├── gripper.py      # 开合夹爪 + 读接触力
│   │   ├── ik.py           # 更稳的 TCP 逆解
│   │   └── scene.py        # 可选场景辅助
│   └── force_data/         # 运行后生成的 CSV / 图片
│
├── urdf/                   # 机器人“零件说明书”
│   ├── reBot-DevArm_fixend_description/  # 任务 B：固定末端
│   └── 00-arm-rs_asm-v3/                  # 任务 B2：带平行夹爪
│
├── README.md               # 安装、命令、交付说明
└── requirements.txt        # Python 依赖
```

### 1.1 分层理解（像搭积木）

```text
┌─────────────────────────────────────────┐
│  脚本层：你运行的 .py（任务流程）          │
│  rebot_sim / 1.力传感 / 2.力控 / 3.滑移   │
└──────────────────┬──────────────────────┘
                   │ 调用
┌──────────────────▼──────────────────────┐
│  工具层 utils：可复用积木                   │
│  robot 加载 | gripper 开合+测力 | ik 求角  │
└──────────────────┬──────────────────────┘
                   │ 调用
┌──────────────────▼──────────────────────┐
│  PyBullet：物理引擎 + 3D 窗口               │
│  碰撞、重力、关节电机、接触力、IK 求解器     │
└──────────────────┬──────────────────────┘
                   │ 读取
┌──────────────────▼──────────────────────┐
│  URDF：连杆、关节、限位、网格外观            │
└─────────────────────────────────────────┘
```

**小白记住一句**：脚本决定“先干什么后干什么”；`utils` 提供“怎么加载臂、怎么捏、怎么算角度”；PyBullet 负责“世界里真的动起来”。

---

## 2. 怎样运行？（从零到出画面）

### 2.1 环境

```bash
# 在仓库根目录
python -m venv .venv
# Windows PowerShell:
.\.venv\Scripts\Activate.ps1

pip install -r requirements.txt
```

依赖主要是：`pybullet`（仿真）、`numpy`（数组）、`matplotlib`（画力曲线）。

### 2.2 任务 B：把手伸到一个点

```bash
# 可选：看末端 link 编号（常见是 6）
python task_B/find_ee.py

# 主程序
python task_B/rebot_sim.py
```

终端提示输入坐标，例如：

```text
0.15, 0.0, 0.15
```

然后 GUI 里机械臂会平滑移过去。

推荐范围（别一上来给太远的点，IK 会失败或姿态很怪）：

- `x`: 0.05 ~ 0.25  
- `y`: -0.15 ~ 0.15  
- `z`: 0.05 ~ 0.25  

### 2.3 任务 B2：力与抓取

建议进入 `task_B2` 再跑（曲线写到本目录 `force_data/`）：

```bash
cd task_B2

# ① 力传感器：抓一下，看正压力时序
python 1.Force_Sensor_Simulation.py

# ② 力控开：铁块+海绵都应能安全抬起
python 2.adaptive_force_control_grasp.py --force-control

# ② 力控关对照：海绵容易被“捏扁”
python 2.adaptive_force_control_grasp.py --no-force-control --only sponge

# ③ 滑移对冲开 / 关
python 3.slip_compensation_test.py --compensate
python 3.slip_compensation_test.py --no-compensate
```

无界面快速自测可加：`--direct --no-show`。

---

## 3. 运行过程：数据流（最重要）

### 3.1 任务 B 一句话流程

```text
你输入目标点 (x,y,z)
    → PyBullet IK：算出 6 个关节角
    → 从当前角线性插值到目标角（很多小步）
    → 每一步：设电机目标 + stepSimulation（世界推进一帧）
    → 末端看起来“平滑飞过去”
```

### 3.2 任务 B2 力控抓取流程

```text
加载带夹爪的臂 + 桌面 + 铁块/海绵
    → 夹爪张开，标定「指心相对手腕」的偏移
    → 移动到物体旁（仍用 IK + 平滑插值）
    → 慢慢闭合，读接触正压力 Fn
    → [力控开] 探一点刚度 → 选 F_safe → PID 维持恒力 → 抬升
    → [力控关] 一直大力闭合 → 海绵可被压扁（演示对比）
```

### 3.3 滑移对冲流程

```text
先力控抓住并抬离桌面
    → 人为加大向下“等效外力”（增强重力）
    → 看相对下滑速度 / 下滑量 / 力异常
    → 对冲开：再收紧一点握宽，提高 Fn
    → 对冲关：握宽不动 → 容易滑落（对照）
```

---

## 4. 每个功能怎么实现？

### 4.1 加载机器人（URDF → 仿真里的刚体链）

**做什么**：把“说明书”变成可仿真的机器人。

**关键文件**：`task_B2/utils/robot.py`

**实现要点**：

1. `p.loadURDF(...)` 读入模型；`useFixedBase=True` 表示底座钉在地上。  
2. 按**名字**找 `joint1~joint6`、夹爪关节、指尖 link（不要写死“第几个一定是夹爪”，换模型更稳）。  
3. 返回一个字典：`robot` id、臂关节列表、末端 link、左右指索引。

**你在干什么**：给后面的 IK、开合、测力一个统一的“机器人句柄”。

---

### 4.2 末端点到点运动（任务 B 核心）

**做什么**：知道手要到 `(x,y,z)`，让整条臂动过去。

**关键文件**：`task_B/rebot_sim.py`

**实现三步**：

1. **读当前关节角** `getJointState`  
2. **IK** `calculateInverseKinematics(robot, 末端link, 目标点)` → 目标关节角  
3. **插值运动**：`for t in 0→1`：中间角 = 当前 + t×(目标−当前)，每步 `POSITION_CONTROL` + `stepSimulation`

这就是最经典的「关节空间轨迹」：不直接指挥笛卡尔直线（更难），而是让关节角平滑变。

---

### 4.3 更好的 IK（任务 B2）

**做什么**：不但到位，还尽量姿态合理（夹爪别朝天、臂别折地上）。

**关键文件**：`task_B2/utils/ik.py`、`2.adaptive_force_control_grasp.py` 里的 `move_tcp`

**实现要点**：

- PyBullet 给出初值后，再用数值方法微调（雅可比迭代，小白可先理解为“一点点改关节，让手更靠近目标”）。  
- 抓取时给一点 **pitch（低头）**，避免某些侧向低位出现“贴地拧巴”解。  
- 求 IK 时短暂关渲染再恢复，避免中间 `resetJointState` 造成画面瞬闪。

---

### 4.4 夹爪开合 + 指心对称（很重要的工程细节）

**做什么**：像真实平行爪一样，两指相对中心对称开合。

**关键文件**：`task_B2/utils/gripper.py`

**为什么特殊**：URDF 里左指行程和右指行程不一样。若简单“同一 width 比例映射”，**指心会漂移**，变成单指先碰到物体 → 左右力差很大。

**实现**：从全开状态两侧**等量回缩**，保持 `q_right - q_left` 恒定 → 指心不漂。

```text
全开 ──等量闭合──▶ 半开 ──再等量──▶ 抓紧
        ↑
   中心线始终在两指中间
```

---

### 4.5 虚拟力 / 触觉传感器

**做什么**：没有真实力传感器芯片，也能读“捏了多重”。

**关键文件**：`1.Force_Sensor_Simulation.py`、`gripper.py`

**两种常用读数**（本项目都用到了）：

| 接口 | 含义 | 小白理解 |
|------|------|----------|
| `getContactPoints` 的法向力 | 接触点**正压力 Fn** | 手指垂直顶着物体的力 |
| `enableJointForceTorqueSensor` + `getJointState` | 关节反作用力/力矩 | 电机“感觉”被顶住了 |

脚本会在闭合过程中**实时打印**，并写入 `force_data/*.csv` 与曲线图。

---

### 4.6 自适应力控抓取

**做什么**：硬物捏紧能提起；软物别捏烂。

**关键文件**：`2.adaptive_force_control_grasp.py`

**状态机式实现**（简化）：

```text
approach  软闭合，直到 Fn ≥ 接触阈值
   ↓
probe     再微微闭合一点，看力涨多快 → 估计刚度 k ≈ ΔF/Δw
   ↓
servo     选 F_safe（硬高 / 软低），PID 让实测力靠近目标
   ↓
hold      维持恒力一小段时间
   ↓
lift      抬升，看物体高度是否上去、力是否塌掉
```

- **力控开**：到安全力就停，铁块、海绵都能抬。  
- **力控关**：无脑继续闭合 → 海绵可出现压扁可视化（对比 Demo）。

场景里物体加载后先 **pin（冻结）**，避免落地弹跳；真正闭合前再解冻，符合“物体在桌上等你去抓”。

---

### 4.7 滑移对冲

**做什么**：提起后向下拽，仍尽量不掉。

**关键文件**：`3.slip_compensation_test.py`（内部复用脚本 2 的抓取工具）

**实现要点**：

1. 抓起后进入“临界握持”（略松一点、摩擦别太大），方便看出差异。  
2. 用**增强重力**模拟向下外力（比乱施加外力更不容易把接触打飞）。  
3. 检测：相对滑移速度、累计下滑量、正压力塌陷、切向力异常等。  
4. `--compensate`：检测到异常就对称收紧握宽 → Fn 上升 → 稳住。  
5. `--no-compensate`：不收紧 → 容易滑落（对照曲线）。

你打开的 `force_data/slip_comp_ON_*.png` 就是：外力上升 → 出现下滑尖峰 → 目标力台阶升高 → 握宽变负（收紧）→ 下滑量停在约几毫米。

---

## 5. 知识点清单（小白版）

下面每个知识点都用「是什么 / 本项目哪里用 / 你先记住什么」说明。

### 5.1 机械臂与自由度（DoF）

- **是什么**：每个独立转动/移动的关节算一个自由度。本臂主体常见 **6 个旋转关节**，再加夹爪开合。  
- **本项目**：`joint1~joint6` + 左右指棱柱关节。  
- **记住**：自由度越多，手能摆的姿态越多，IK 也越可能有多解（同一目标点多种弯法）。

### 5.2 连杆与关节；URDF

- **是什么**：URDF 用 XML 描述：哪节骨头连哪节、关节类型、限位、碰撞网格。  
- **本项目**：`urdf/` 下两套模型（固定末端 / 带夹爪）。  
- **记住**：仿真里的“机器人”= URDF + 物理参数（质量、摩擦等）。

### 5.3 正运动学 vs 逆运动学（FK / IK）

- **正运动学**：已知关节角 → 算手在哪（容易）。  
- **逆运动学**：已知手要在哪 → 算关节角（难，常数值迭代）。  
- **本项目**：几乎所有“去某个点/去抓物体”都靠 IK。  
- **记住**：IK 可能失败（够不着）、或多解（姿态怪）；要限制工作空间、选合理姿态。

### 5.4 关节空间插值与平滑运动

- **是什么**：不让关节角一步跳到目标，而是中间走很多小步。  
- **本项目**：`np.linspace` / 五次多项式 smoothstep + 每步仿真。  
- **记住**：看起来平滑 = 插值够密 + 每步都 `stepSimulation`；否则像瞬移或抖动。

### 5.5 位置控制电机

- **是什么**：告诉仿真器“这个关节请尽量转到 θ”，内部用 PD 一类控制去跟目标。  
- **本项目**：`setJointMotorControl2(..., POSITION_CONTROL, ...)`。  
- **记住**：力控抓取时，夹爪常仍用位置（改宽度），用接触力当反馈来决定“宽度要不要再减小”。

### 5.6 接触、摩擦、正压力

- **正压力 Fn**：垂直接触面的力。  
- **摩擦力**：沿切向，阻止相对滑动，大致与 Fn、摩擦系数有关。  
- **本项目**：抓取看 Fn；滑移对冲同时看切向/相对速度。  
- **记住**：提起物体靠的是**摩擦**，所以 Fn 太小会滑。

### 5.7 刚度（软硬）直觉

- **是什么**：同样多捏一点（Δw），力涨多少（ΔF）。硬物 ΔF 大，软物 ΔF 小。  
- **本项目**：probe 阶段估 `k ≈ ΔF/Δw`，硬物给高 `F_safe`，软物给低。  
- **记住**：自适应的关键不是“固定 20N”，而是“先摸摸有多硬再决定”。

### 5.8 PID 控制（力环）

- **是什么**：根据误差（目标力 − 实测力）决定怎么调输入（这里主要是再闭合一点还是松开一点）。  
  - P：误差大就多调  
  - I：长期差一点就慢慢补  
  - D：变化太快就抑制抖动  
- **本项目**：力伺服阶段用 PID 调握宽。  
- **记住**：PID 是反馈控制的入门砖；参数不对会抖或跟不上。

### 5.9 物理仿真步进

- **是什么**：世界不是连续的，而是一帧帧算：碰撞、重力、积分。  
- **本项目**：`p.setGravity`、`p.stepSimulation`、固定 `SIM_DT`（如 1/240 s）。  
- **记住**：你的控制循环通常是“读传感器 → 算指令 → step 几步”。

### 5.10 坐标系与 TCP

- **TCP（Tool Center Point）**：工具中心点，常取夹爪中心或指心。  
- **本项目**：B2 里常用**两指中点**当抓取对准目标，再反推手腕该在哪。  
- **记住**：说“到 (0.3,0,0.2)”必须说清楚是哪个坐标系、哪个点。

---

## 6. 带注释的最小示例（任务 B 核心逻辑）

下面是理解 `task_B/rebot_sim.py` 时应对齐的“骨架”（注释比代码更重要）：

```python
# 1) 连接仿真世界，打开 3D 窗口，设置重力
p.connect(p.GUI)
p.setGravity(0, 0, -9.8)

# 2) 加载 URDF：得到 robotId（之后所有 API 都用它）
robotId = p.loadURDF(urdf_path, useFixedBase=True)

# 3) 找出可动关节（跳过 FIXED）
joint_indices = [...]
end_effector_index = 6   # 末端 link：手先找到再写死/配置

# 4) 你给笛卡尔目标；机器人需要关节目标
target_pos = [x, y, z]
current_angles = [p.getJointState(robotId, i)[0] for i in joint_indices]

# 5) IK：笛卡尔 → 关节
joint_poses = p.calculateInverseKinematics(
    robotId, end_effector_index, target_pos,
    maxNumIterations=500,
)

# 6) 关节空间插值：平滑，而不是一步跳变
for t in np.linspace(0, 1, steps):
    interp = current + t * (target - current)
    p.setJointMotorControlArray(
        robotId, joint_indices, p.POSITION_CONTROL,
        targetPositions=interp,
    )
    p.stepSimulation()   # 推进物理世界一帧
    time.sleep(0.01)     # 让人眼跟得上（实时观感）
```

任务 B2 只是在这套骨架外，又加了：**夹爪宽度指令、接触力读取、根据力改宽度、抬升与扰动**。

---

## 7. 建议学习路线（小白 3 步）

1. **只跑任务 B**  
   改坐标、改 `steps`，观察：步数少会怎样？点太远会怎样？

2. **跑力传感器脚本**  
   盯终端里的力数字和 `force_data` 曲线：闭合时 Fn 何时从 0 变大？

3. **跑力控开/关 + 滑移开/关**  
   同一场景对比：关力控捏扁；开力控安全抓；关对冲掉落；开对冲微滑后稳住。

每做一步，对照本文第 4、5 节问自己：  
**这一帧读了什么？算了什么？写给电机/夹爪的指令是什么？**

---

## 8. 常见困惑速答

| 困惑 | 简答 |
|------|------|
| 为什么不直接控制 xyz 电机？ | 真实臂电机在关节上；要先 IK 变成关节角。 |
| 为什么有时姿态很奇怪？ | IK 多解；需要姿态约束/种子/惩罚贴地折叠。 |
| 力控是不是必须用力矩电机模式？ | 本仿真用“调握宽 + 读接触力”的力闭环，工程上很常见。 |
| 仿真和真机一样吗？ | 原理通，但摩擦、柔性、传感器噪声在真机更难；仿真是练流程的沙盘。 |
| README 和本文啥关系？ | README 偏安装与交付；本文偏**讲清楚在干什么**。 |

---

## 9. 你做完这些功能后，简历上能怎么说（可选）

- 基于 PyBullet 搭建六轴臂仿真，完成笛卡尔点到点 IK 平滑运动。  
- 部署接触正压力虚拟传感，记录抓取力时序。  
- 实现刚度估计 + 力 PID 的自适应抓取（硬/软物体）。  
- 实现抬升后扰动下的滑移检测与握力补偿对照实验。

---

## 10. 下一步可以去读的文件

| 优先级 | 文件 | 读什么 |
|--------|------|--------|
| ★★★ | `task_B/rebot_sim.py` | 最短闭环：输入→IK→插值 |
| ★★★ | `task_B2/utils/gripper.py` | 开合映射 + 力读取 |
| ★★☆ | `task_B2/utils/robot.py` | 如何从名字解析关节 |
| ★★☆ | `task_B2/2.adaptive_force_control_grasp.py` | 力控状态机（先搜 `force_controlled_grasp`） |
| ★☆☆ | `task_B2/3.slip_compensation_test.py` | 对冲检测与加紧 |
| ★☆☆ | `task_B2/utils/ik.py` | IK 精调（有余力再看） |

---

## 附录 A：零线代版——坐标与 IK（直觉优先）

> 写给「一看到旋转矩阵就晕」的读者。  
> **目标**：不证明定理，也能对着本仓库的 GUI 把话讲清楚。  
> **标准**：会画、会说、会改参数做实验；公式以后再回填。

### A.1 先换验收标准

| 现阶段「懂了」是指 | 暂时不必 |
|--------------------|----------|
| 能指出：桌子 / 基座 / 手腕 / 指心大概在哪 | 会手推旋转矩阵 |
| 能说出：FK 与 IK 谁是「正向」、谁是「反向」 | 会写解析 IK |
| 知道怪姿态、够不着是 IK 的常态 | 会选最优解的严格算法 |
| 知道平滑运动 = 关节角慢慢变 | 会最优时间轨迹 |

你已经跑过 `task_B/rebot_sim.py`：那就是 IK + 插值。缺的是**语言和图像**，不是天赋。

---

### A.2 坐标系 = 贴在零件上的「小纸条」

忘掉矩阵三天。想象每个关键部位贴了一张纸条，上面画着：

- **前方**（常叫 X）  
- **侧方**（常叫 Y）  
- **上方**（常叫 Z）  

| 纸条贴在哪 | 在本项目里像什么 | 你为什么关心它 |
|------------|------------------|----------------|
| 地面 / 世界 | 蓝格子地板 | 你输入的 `(x,y,z)` 多半相对它 |
| 基座 | 臂最底下那块板 | 整台臂从这里「长出来」 |
| 末端 / 手腕附近 | `ee_link` / `gripper_end` | IK 说的「手到哪」常指这里 |
| 两指中间（指心） | 左右指尖连线中点 | 抓方块时，对准的是**物体中心**，不是随便一个点 |

**「坐标变换」用人话讲**：

> 同一件事，用不同纸条来描述。  
> 「杯子在桌子右前方 20cm」和「杯子在我鼻子前方 30cm」说的可以是同一个杯子——纸条不同，数字不同。

机械臂里，电脑要不停地在这些纸条之间翻译。  
你暂时只要会问：**这句话是站在哪张纸条上说的？**

**纸笔练习（5 分钟）**  
画三个框：`桌子`、`基座`、`手`。  
用箭头写：「手在桌子的 ______」。不写公式，只写前后左右上下。

---

### A.3 在本仓库 GUI 里认纸条（强烈建议真做）

```bash
python task_B/rebot_sim.py
# 输入例如：0.15, 0.0, 0.15
```

边看边问：

1. 数字变大的那个轴，手是远离你还是升高？  
2. 只改 `y`（如 `0.15, 0.10, 0.15`），手往哪侧偏？  
3. `z` 太小会怎样？（可能撞地或 IK 很别扭）

任务 B2 抓取时还有两张更细的纸条：

- **手腕（EE）**：臂最后一个可动端  
- **指心**：两指中点——`2.adaptive_force_control_grasp.py` 里对准物体用的是指心，再反推手腕该在哪（代码里的 `ee_for_mid` / 标定偏移）

人话：

> 我想让**指心**对准方块中心；  
> 但电机只能控制**关节**；  
> 所以要先知道「指心相对手腕偏了多少」，再让 IK 去够那个手腕位姿。

---

### A.4 FK 与 IK：只记箭头方向

```text
        容易（拼积木）
关节角 ──────────────▶ 手在空间中的位置/姿态
         正运动学 FK

        难（常靠电脑猜）
目标位置 ◀────────────── 关节角
         逆运动学 IK
```

| | FK | IK |
|--|----|----|
| 问法 | 关节弯成这样，手在哪？ | 手要到这里，关节怎么弯？ |
| 本项目哪里 | 仿真每帧都在算（你看不见） | `rebot_sim.py` 里 `calculateInverseKinematics`；B2 的 `move_tcp` |
| 难点 | 相对简单 | 可能**多解**（多种弯法）或**无解**（够不着） |

**为什么你会看到「拧巴姿态」？**  
IK 找到的是「数学上够得着」的一种弯法，不一定是「人看起来自然」的那种。  
所以 B2 里才会加一点低头（pitch）、多种子、惩罚贴地折叠——都是在**挑好看/能用的解**，不是你没学懂线代。

**对照实验**

1. 工作空间内一点：`0.15, 0, 0.15` → 通常成功。  
2. 很远：`0.5, 0.5, 0.5` → 容易失败或姿态怪。  
3. 同一点多跑两次 → 有时弯法略有不同（多解味道）。

---

### A.5 轨迹：就是动画补间

关节现在 \(30°\)，目标 \(60°\)：

- **一步跳到 60°** → 画面瞬移（坏）  
- **30→33→36→…→60** → 平滑（好）

`np.linspace(0, 1, steps)` + 每步 `stepSimulation` 就是这个。  
`steps` 改小 / 改大，立刻能看见差别——**这和线代无关**。

---

### A.6 「简单控制」一句话

**位置控制**：  
「关节现在是 40°，我命令 50°，请往 50° 靠。」

**力反馈调夹爪**（你在 B2 里做的）：  
「压力还不够 → 再合拢一点；到了 → 停。」

先别碰传递函数；把控制理解成 **看见误差就修正** 即可。

---

### A.7 线代最小补习（真要用时再看）

只需要建立这四个图像（看动画，别背定义）：

1. **向量** = 带箭头的量（方向 + 长度）  
2. **相加** = 箭头首尾相接  
3. **2D 旋转** = 箭头绕原点转一个角度  
4. **矩阵×向量** = 「按规则把旧箭头变成新箭头」

推荐：3Blue1Brown《线性代数的本质》前 4–5 集。  
每天 20 分钟视频 + 10 分钟盯着你的臂 GUI，比硬啃教材第 2 章顺。

**暂时跳过**：特征值、SVD、抽象线性空间……做当前项目用不上。

以后课本里出现 \(R\)、\(T\)，翻译成：

> \(R\)：怎么转；\(T\)：怎么转再挪。  
> 下标常表示「从谁变到谁」——先问清这两张纸条是谁。

---

### A.8 专治劝退的 7 天小计划

| 天 | 做什么 | 怎样算过关 |
|----|--------|------------|
| 1–2 | 只跑 `task_B/rebot_sim.py`，换 5 个点 | 能口述「我在做 IK」 |
| 3 | 看 3B1B「向量 + 线性变换」 | 能说「矩阵是在变形箭头」 |
| 4 | 纸上画桌子/基座/手三张纸条 | 不写公式能讲「在哪边」 |
| 5 | `steps=20` 与 `steps=200` 对比 | 能解释什么是插值轨迹 |
| 6 | 读 `task_B2/utils/gripper.py` 文头注释 | 能讲指心为什么不能漂 |
| 7 | 跑力控，只盯「接触后力变大」 | 能讲「根据力再决定合不合」 |

七天后矩阵符号仍讨厌 → **完全正常**。继续以仿真为主教材，符号见到再回填。

---

### A.9 一张总图（贴墙上）

```text
你输入 (x,y,z)          ← 世界 / 桌子这张纸条上的数字
        │
        ▼
      IK 求解            ← 电脑猜关节角（可能多解/失败）
        │
        ▼
  关节角平滑插值         ← 动画补间，别一步跳变
        │
        ▼
  电机位置控制 + 仿真步进 ← 臂在画面里动起来
        │
        ▼
  （B2）指心对准物体 → 读压力 → 调握宽 → 抬起 / 防滑
```

**口诀**：  
纸条说位置 → IK 变关节 → 插值变平滑 → 力觉变聪明。

---

**一句话收尾**：  
这个项目是在电脑里养了一条虚拟机械臂——先学会**伸手到位**，再学会**用“捏的力”做闭环**，最后学会**东西要滑时再加一点力**。你现在不需要一次懂所有公式；先会跑、会对着画面说出每一步在干什么，就已经入门了。坐标变换也一样：**先认纸条，再认公式**。
