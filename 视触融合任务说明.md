# 视触融合智能自适应抓取 — 任务说明与学习导读（v3 规划）

> **读者**：机械臂 / 具身智能新手  
> **目的**：在你**主导**下，用 AI 写代码完成「视觉定位 → 力控抓取 → 分拣放置」全流程；先懂架构与知识点，再让 AI 实现，而不是让 AI 乱改一通（反 vibe coding）。  
> **约束**：本文只讲「做什么、为何、用什么、怎么理解」；**不直接改仓库代码**。  
> **前置**：你已有任务 B（点到点 IK）与任务 B2（力传感 / 自适应力控 / 滑移对冲）。基础概念可对照 [`新手入门讲解.md`](新手入门讲解.md)。

---

## 0. 一句话：这次任务要做成什么？

把已经会的 **「力控抓取（B2）」** 和新增的 **「相机看物体在哪（CV）」** 接到一起，在仿真里做到：

```text
相机看见物体 → 算出物体在臂坐标系下的 (x,y,z)
    → IK 平滑飞到预抓取点
    → 下探接触，切入自适应力控（硬捏紧 / 软轻捏）
    → 按类别放到指定区域（分拣）
（进阶）抓取过程中物体被挪走 → 视觉重跟踪再去抓
```

这就是题目说的 **「视触融合」**：  
- **视** = 眼睛（RGB-D / OpenCV）告诉你「去哪」  
- **触** = 力觉（你 B2 已有的正压力 / 刚度估计）告诉你「捏多重」

---

## 1. 你现在已经有什么？（不要推倒重来）

### 1.1 当前仓库能力地图

| 已有模块 | 路径 | 已经能干什么 | 新任务里怎么复用 |
|----------|------|--------------|------------------|
| 点到点运动 | `task_B/rebot_sim.py` | 输入 `(x,y,z)` → IK → 平滑插值 | 理解「笛卡尔目标 → 关节运动」 |
| 加载带夹爪臂 | `task_B2/utils/robot.py` | 解析关节 / 指尖 link | 新工程继续 `load_robot()` |
| 指心开合 + 读力 | `task_B2/utils/gripper.py` | 对称开合、`getContactPoints` 正压力 | 力控闭合与接触判定 |
| 稳妥 IK / TCP | `task_B2/utils/ik.py` | 姿态约束 + 精调 | 预抓取点、放置点运动 |
| 力传感演示 | `task_B2/1.Force_Sensor_Simulation.py` | 力时序 CSV/曲线 | 调试「接触瞬间」 |
| 自适应力控 | `task_B2/2.adaptive_force_control_grasp.py` | 探刚度 → `F_safe` → PID → 抬升 | **精接触模块直接复用/抽取** |
| 滑移对冲 | `task_B2/3.slip_compensation_test.py` | 扰动下加紧 | 可选：搬运途中防滑 |

### 1.2 新任务相对 B2 缺什么？

| 缺口 | 说明 |
|------|------|
| 虚拟相机 | B2 物体坐标是**写死/固定**的，没有「看见」 |
| 视觉检测与 3D 定位 | 要从图像/深度得到物体 `(x,y,z)` |
| 手眼关系 | 相机坐标系 → 机械臂基座坐标系 |
| 流水线编排 | 视觉 → 靠近 → 力控 → 放置 的状态机 |
| 分拣逻辑 | 硬/软（或红/黄）放到两个不同目标区 |
| （进阶）重跟踪 | 下探过程中目标移动，视觉更新轨迹 |

**主导原则**：新代码主要加「眼睛 + 流水线」；**力控大脑尽量调用 B2，而不是重写一套。**

---

## 2. 建议的目标工程架构（你主导 AI 时按这个拆）

交付题目要求整理 **Vision-Tactile-Grasping** 工程。建议在仓库中新增一层（名称可自定，下列为推荐）：

```text
my-reBot-DevArm/
├── task_B/                          # 保持不动：运动学入门
├── task_B2/                         # 保持不动：力控能力库（被新任务 import）
├── task_VTG/                        # ★ 新增：视触融合（Vision-Tactile Grasping）
│   ├── README.md                    # 本任务运行说明 + 架构图
│   ├── main_pipeline.py             # 全流程状态机入口（Demo 3.0）
│   ├── config.py                    # 相机位姿、放置区、颜色/刚度标签
│   ├── vision/
│   │   ├── camera.py                # 挂载 RGB-D、取 RGB/深度/点云
│   │   ├── detect.py                # 分割/检测 → 像素 + 深度 → 相机系 3D
│   │   └── handeye.py               # 相机系 → 基座系（手眼变换）
│   ├── motion/
│   │   ├── approach.py              # 预抓取点、下探、放置轨迹（调用 B2 IK）
│   │   └── retreat.py               # 抬升、回安全位
│   ├── grasp/
│   │   └── adaptive_bridge.py       # 薄封装：调用 B2 力控闭合/抬升
│   ├── sort/
│   │   └── place_zones.py           # 易碎区 / 重物区坐标与放置策略
│   ├── tracking/                    # 进阶：动态重跟踪
│   │   └── retrack.py
│   └── force_data/                  # 本任务运行产出
├── urdf/
├── 新手入门讲解.md
├── 视触融合任务说明.md               # ← 本文
└── README.md                        # 总述里链到 task_VTG
```

### 2.1 系统架构图（逻辑）

```text
                    ┌──────────────────┐
                    │  RGB-D 虚拟相机   │
                    │  (Eye-to-Hand /  │
                    │   Eye-in-Hand)   │
                    └────────┬─────────┘
                             │ RGB + Depth
                             ▼
                    ┌──────────────────┐
                    │ 视觉：检测+3D定位 │
                    │ OpenCV / 点云    │
                    └────────┬─────────┘
                             │ 物体在「相机坐标系」
                             ▼
                    ┌──────────────────┐
                    │ 手眼变换 handeye │
                    │ cam → base       │
                    └────────┬─────────┘
                             │ 物体在「基座坐标系」(x,y,z)
                             ▼
              ┌──────────────┴──────────────┐
              │     流水线状态机 pipeline      │
              │  idle→detect→approach→      │
              │  contact→force_grasp→       │
              │  lift→place→next            │
              └───────┬───────────┬─────────┘
                      │           │
         粗定位/轨迹   │           │ 精接触/力控
                      ▼           ▼
              ┌───────────┐ ┌─────────────────┐
              │ IK 平滑运动 │ │ B2 自适应力控     │
              │ (复用 B2)  │ │ 探刚度→F_safe→PID │
              └───────────┘ └────────┬────────┘
                                     │
                                     ▼
                            ┌─────────────────┐
                            │ 分拣放置两区域    │
                            │ 软→区A  硬→区B   │
                            └─────────────────┘
```

### 2.2 你怎么用 AI coding（而不是 vibe coding）

| 做法 | 说明 |
|------|------|
| **一次只开一个模块** | 例如先只做 `camera.py` 能截图，再做检测，再做手眼 |
| **先写接口再写实现** | 你规定函数输入输出，再让 AI 填身体 |
| **验收标准先写清** | 「深度图非空」「基座系误差 < 2cm」等 |
| **禁止一次 PR 改遍全仓库** | B2 只允许「被 import」或抽公共函数，不顺手大重构 |
| **每次让 AI 解释 diff** | 你要能复述：改了哪一层、数据从哪到哪 |
| **用本文当提示词附录** | 「按 视触融合任务说明.md 第 X 节实现，不要越界」 |

**你主导的检查句**（每次合并前问自己）：

1. 这一步的输入是什么？输出是什么？  
2. 数据在哪个坐标系？  
3. 失败时怎么表现（检测丢了、IK 失败、力一直为 0）？  
4. 是否复用了 B2，而不是复制粘贴改烂？

---

## 3. 交付要求对照（你要完成到什么程度）

| 交付物 | 内容 | 建议验收 |
|--------|------|----------|
| GitHub v2.0 / VTG 工程 | 完整 Vision-Tactile 目录 + 架构图 + README | 他人按 README 能跑通 Demo |
| Video Demo 3.0 画面一 | 相机画面上标出物体 3D 坐标 | 可见框/点 + 文字坐标 |
| Video Demo 3.0 画面二 | 飞向目标 → 接触切力控 → 轻抓易碎品 → 分类放置 | 无手动改物体坐标 |
| 进阶（加分） | 下探中挪动物体，能重跟踪 | 日志打印「retrack」并最终仍抓住 |

---

## 4. 核心攻关模块：如何完成？

下面按题目三大块拆解：**任务步骤 → 用到的知识点/技术 → 实现思路 → 定义与理解法**。

---

### 模块 1：视觉感知与目标定位（Visual Guidance）

#### 4.1.1 要完成的任务步骤

1. 在 PyBullet（你当前栈）场景中挂载 **虚拟 RGB-D 相机**。  
2. 工作台随机摆放至少两类物体：例如 **红色刚体块**、**黄色易碎物（纸杯/海绵）**。  
3. 用视觉脚本得到物体在相机坐标系下的 3D 点，再变到 **机械臂基坐标系**。  
4. （演示）在图像上画出检测框 / 投影点，并打印 `(x,y,z)_base`。

#### 4.1.2 布局选择：Eye-to-Hand vs Eye-in-Hand

| 布局 | 定义 | 优点 | 缺点 | 新手建议 |
|------|------|------|------|----------|
| **Eye-to-Hand** | 相机固定在场景（旁视/顶视），不装在手腕上 | 标定一次即可；实现简单 | 可能有遮挡 | **先做这个** |
| **Eye-in-Hand** | 相机装在末端，跟着手臂动 | 近看更清 | 每帧外参随臂变，调试难 | 第二阶段再做 |

**人话**：  
- Eye-to-Hand = 房间天花板上的监控摄像头。  
- Eye-in-Hand = 头上绑的运动相机。

#### 4.1.3 用到的知识点 / 技术（定义 + 怎么理解）

**① RGB-D 相机**

- **定义**：同时输出彩色图（RGB）和每个像素到相机的距离（Depth）。  
- **理解**：RGB 告诉你「像什么颜色/形状」；Depth 告诉你「有多远」。两者对齐后才能得到 3D。  
- **本项目技术**：PyBullet `getCameraImage`（可拿到 rgb、depth、分割 mask）。

**② 针孔相机模型（先建立直觉即可）**

- **定义**：把 3D 点通过「小孔」投影到 2D 图像平面的几何模型；内参（焦距、主点）描述投影关系。  
- **理解**：同一物体，离相机越远，在图像上越小；深度 + 像素 + 内参 → 可反推出相机坐标系下的 3D 点。  
- **公式不必先会推**：先会用「像素 `(u,v)` + 深度 `d` + 内参 → 点 `(X,Y,Z)_cam`」的现成函数。

**③ 目标检测 / 分割（本任务可用简单版）**

- **定义**：从图像中找出感兴趣物体的位置（框或像素集合）。  
- **理解**：新手可用 **颜色阈值**（红/黄）在仿真里稳定工作；以后再换 YOLO 等。  
- **技术**：OpenCV（`cv2.inRange`、找轮廓、算质心）；或 PyBullet 自带 segmentation mask（仿真作弊但合法练手眼）。

**④ 深度图 → 3D 点**

- **定义**：对检测出的像素，读取深度，反投影得相机系坐标。  
- **理解**：先找「物体在图上的中心像素」，再问「这一像素有多深」，合成 3D。  
- **坑**：深度噪声、物体边缘深度乱跳 → 对 mask 内部取中位数深度更稳。

**⑤ 坐标系与刚体变换（手眼标定概念）**

- **定义**：用旋转 + 平移描述「从一个坐标系变到另一个坐标系」。手眼标定是求相机相对臂（或基座）的固定/可变外参。  
- **理解（接《新手入门讲解》附录 A）**：相机一张纸条，基座一张纸条；手眼 = 两张纸条之间的翻译规则。  
- **Eye-to-Hand 简化**：你在仿真里**自己设定**相机位姿 → 外参已知，不必做真实标定板；但仍要写清 `T_base_cam`（相机→基座）变换，概念才算做对。  
- **技术**：`numpy` 矩阵；或四元数/欧拉角与 PyBullet 的 `multiplyTransforms` / `invertTransform`。

**⑥ 随机摆放**

- **定义**：物体位姿在合理范围内采样，避免每次写死同一点。  
- **理解**：证明视觉闭环，而不是「记得物体在 0.38,-0.1」。

#### 4.1.4 实现思路（给你主导 AI 的施工顺序）

```text
Step V1  camera.py：固定顶视相机，保存一张 RGB + Depth（能看图即可）
Step V2  detect.py：红/黄阈值 → 像素中心；用 Depth 得 (X,Y,Z)_cam
Step V3  handeye.py：用已知相机位姿变到 (x,y,z)_base；与 PyBullet 真实 getBasePosition 对比误差
Step V4  可视化：OpenCV 画框 + 在图上写 base 坐标（Demo 画面一）
Step V5  随机生成两个物体位姿，循环检测仍正确
```

**验收**：对静止物体，视觉给出的基座系 XY 误差建议 **< 1~2 cm**（仿真简单场景可更严）。

---

### 模块 2：「视触融合」抓取流水线（Vision-Tactile Pipeline）

#### 4.2.1 要完成的任务步骤

1. **粗定位（视觉）**：用模块 1 的 `(x,y,z)_base`，规划预抓取点（通常在物体正上方一定高度），IK 平滑移动。  
2. **精接触 + 力控**：向下接近；当接触力超过阈值，切入 B2 自适应力控（探刚度 → `F_safe` → 恒力）。  
3. **搬运分拣**：抬起后，软/易碎 → 区域 A；硬/重 → 区域 B；放下、松爪、回安全位；处理下一个。

#### 4.2.2 流水线状态机（建议你强制 AI 按此实现）

```text
IDLE
  → DETECT          # 视觉列出待抓列表（含类别标签 soft/hard）
  → SELECT_NEXT     # 选一个目标
  → APPROACH_PRE    # 移到预抓取点（上方）
  → DESCEND         # 缓慢下探
  → WAIT_CONTACT    # 监控 Fn，超过阈值则切换
  → FORCE_GRASP     # 调用 B2 自适应力控闭合
  → LIFT            # 抬升验证
  → TRANSPORT       # 移到对应放置区上方
  → PLACE           # 下降放置、张开
  → RETREAT         # 抬起离开
  → DETECT 或 DONE
```

失败分支（必须有，否则是 vibe）：

- `DETECT` 找不到 → 重试 / 结束并报错  
- `APPROACH` IK 失败 → 换姿态或跳过该物体  
- `WAIT_CONTACT` 超时无力 → 视为定位偏差，允许视觉重测一次  
- `LIFT` 掉落 → 记录失败，不阻塞下一物体（Demo 可简化）

#### 4.2.3 用到的知识点 / 技术

**① 预抓取点（Pre-grasp）**

- **定义**：抓取前先到达的安全位姿，通常在物体上方，夹爪张开。  
- **理解**：先「对准再下去」，避免斜着撞翻。  
- **实现**：`p_pre = p_obj + [0,0,h]`（h 如 0.12~0.18 m），再 `move_tcp`。

**② 粗定位 vs 精接触**

- **定义**：粗 = 视觉把误差收到厘米级；精 = 用接触/力把相对关系收到「贴住并能捏」。  
- **理解**：眼睛负责「走到附近」，皮肤负责「贴紧并控制力」——融合的意义在此。

**③ 接触检测**

- **定义**：判断末端/手指是否与环境发生力交互。  
- **技术**：复用 B2 的 `getContactPoints` 正压力；阈值如 `Fn > 1~2 N`。  
- **理解**：比「看深度是否碰到」更直接，和真机力控习惯一致。

**④ 自适应力控（你已具备）**

- **定义**：根据接触表现出的软硬（刚度）选择不同安全力并维持。  
- **理解**：硬物 → 大力才能提起；软物 → 大力会捏扁。  
- **实现**：`FORCE_GRASP` 状态 **调用** `2.adaptive_force_control_grasp.py` 中的力控函数，而不是重写 PID。  
- **类别来源**：可用视觉颜色先验（黄=软、红=硬），也可用探刚度结果校验（更「触」）。

**⑤ 轨迹规划（本阶段的含义）**

- **定义**：广义上是生成从 A 到 B 的可行运动；你现阶段 = **关节空间平滑插值 / 多点经由**（预抓取→接触→抬升→放置）。  
- **理解**：先保证点序正确 + IK 可达 + 插值平滑；不必上 MoveIt。  
- **技术**：复用 B2 `move_tcp`、via 点（先抬高再横移，防扫地）。

**⑥ 分拣（Sorting）**

- **定义**：按属性把物体运到不同目标区域。  
- **实现**：`config.py` 里两个放置区中心；`soft → zone_A`，`hard → zone_B`。  
- **理解**：闭环的「任务层」逻辑，和底层 IK/力控分层。

#### 4.2.4 实现思路（施工顺序）

```text
Step P1  不用视觉：手动给一个 base 坐标，跑通 approach→contact→B2力控→place（管道先通）
Step P2  换成视觉坐标，仍单物体
Step P3  两物体 + 两放置区，全自动 for 循环
Step P4  Demo 叠加：第二窗口或同窗口标注坐标；录屏
Step P5  整理 task_VTG/README 架构图与命令
```

**验收**：无终端输入物体坐标；一次运行完成「检测→抓→分类放置」至少 2 个物体。

---

### 模块 3：动态扰动抗干扰（进阶）

#### 4.3.1 要完成的任务步骤

在 **DESCEND / APPROACH** 过程中，随机平移物体；系统周期性跑视觉，更新目标预抓取点，重新规划，再去抓。

#### 4.3.2 用到的知识点 / 技术

**① 视觉伺服 / 重跟踪（简化版）**

- **定义**：用视觉反馈不断修正运动目标。经典分位置伺服与图像伺服；你做 **位置级重跟踪** 即可。  
- **理解**：不是一次看清就闭眼走，而是「走两步看一眼」。  
- **实现**：每 N 帧（或每 M 厘米移动）`detect + handeye`，若目标 XY 变化超过阈值，则更新 `p_pre` 并改 IK 目标。

**② 控制频率与实时性**

- **定义**：多快更新一次感知与指令。  
- **理解**：仿真里可用 5~20 Hz 重检测；太慢跟丢，太快算不过来。  
- **技术**：状态机里加 `last_detect_time`；运动用短 horizon 插值，便于中途改目标。

**③ 稳健性**

- **定义**：感知噪声、短暂丢检时系统不崩。  
- **实现**：丢检则保持上一目标短暂继续；连续丢检则上升回安全位再搜。

#### 4.3.3 实现思路

```text
Step T1  下探循环中打印「真实物体位置 vs 当前目标」
Step T2  手动/脚本在下探中挪物体，触发 retrack 日志
Step T3  调阈值：位移 > 2cm 才重规划，避免抖动
```

**验收**：Demo 中可见物体被挪动后，臂改变下降方向并仍抓住（允许变慢）。

---

## 5. 知识点总表（备考式）

| 名称 | 一句话定义 | 在本任务中的角色 | 如何建立理解 |
|------|------------|------------------|--------------|
| URDF / 仿真 | 虚拟机器人与物理世界 | 场景载体 | 跑现有 B/B2 |
| RGB-D | 彩色+深度 | 看见物体与距离 | 保存一张深度图看灰度 |
| 检测/分割 | 找出物体像素 | 得到 `(u,v)` | 先颜色阈值 |
| 反投影 | 像素+深度→相机系 3D | 得到 `(X,Y,Z)_cam` | 固定一个像素试算 |
| 坐标系变换 | 不同「纸条」间翻译 | 相机→基座 | 附录 A 纸条比喻 |
| 手眼关系 | 相机相对臂的外参 | 定位可用 | Eye-to-Hand 先用已知外参 |
| FK / IK | 角↔位姿 | 运动到预抓取/放置 | 任务 B |
| 预抓取 | 物体上方安全点 | 粗定位 | 先上方再下探 |
| 接触力 / 正压力 | 垂直接触面的力 | 触发力控、判接触 | B2 力曲线 |
| 刚度 | 多捏一点力涨多少 | 软硬自适应 | B2 probe |
| PID 力控 | 用误差调输入维持目标力 | 抓紧不捏烂 | B2 servo |
| 状态机 | 按状态转移的流程控制 | 全流程编排 | 画出模块 2 状态图 |
| 分拣 | 按类送到不同区 | 任务完成定义 | 两个盒子 |
| 重跟踪 | 目标动了再更新 | 抗干扰 | 走两步看一眼 |

---

## 6. 推荐技术栈（与现有项目对齐）

| 层级 | 推荐 | 原因 |
|------|------|------|
| 仿真 | **继续 PyBullet** | 与 B2 一致，降低切换成本 |
| 视觉 | OpenCV + NumPy | 颜色检测足够交 Demo；以后可换深度学习 |
| 深度 | `getCameraImage` 的 depth | 不必先上真实相机驱动 |
| 运动/力控 | **复用 `task_B2/utils` + 力控函数** | 满足「与 B2 打通」 |
| 可选升级 | MuJoCo / YOLO / ROS2 | 交完 v3 Demo 再考虑，避免并行劝退 |

依赖可能新增：`opencv-python`（写进将来 `requirements.txt` 时由你确认）。

---

## 7. 建议实施里程碑（你带 AI 的排期）

| 里程碑 | 目标 | 你怎么验收（主导） |
|--------|------|-------------------|
| M0 | 读完本文 + 画出自己的状态机草图 | 能向别人讲清数据流 |
| M1 | 顶视 RGB-D + 存图 | 文件夹里有 rgb.png / depth 可视化 |
| M2 | 红黄检测 → base 坐标，误差对比 GT | 打印误差表 |
| M3 | 无视觉管道：固定坐标跑通抓取+分拣 | 两物体进两区 |
| M4 | 视觉坐标接入管道 | **不手改坐标** 仍成功 |
| M5 | Demo 画面标注 + 录屏 3.0 | 对照题目两条画面 |
| M6 | （进阶）下探重跟踪 | 日志 + 录像 |
| M7 | README 架构图 + 版本 tag | 他人可复现 |

每完成一个里程碑再让 AI 开下一个；**不要**一句话「帮我把视触融合全做完」。

---

## 8. 给 AI 的提示词

完整模板、里程碑专用提示词、审查清单与控场话术见：

→ [**本工程如何用AI开发（提示词说明）.md**](本工程如何用AI开发（提示词说明）.md)

最小可用句式：

```text
请严格按仓库《视触融合任务说明.md》实现【模块名 / 里程碑 Mx】。
约束：
1. 只新增/修改 task_VTG/ 下相关文件，不要重构 task_B2 整体逻辑；
2. 力控必须复用 task_B2 已有能力（说明 import 哪个函数）；
3. 先给出：文件列表、函数签名、输入输出坐标系；
4. 再写代码；最后列出我该如何手动验收的 3 条命令/现象；
5. 不要添加与本 Step 无关的功能（如 YOLO、ROS）。
```

---

## 9. 风险与常见坑（提前知道就不慌）

| 坑 | 原因 | 处理 |
|----|------|------|
| 视觉坐标差很大 | 手眼矩阵方向反了（cam→base vs base→cam） | 与 `getBasePosition` 对拍 |
| 深度在边缘乱 | 掩膜含背景 | 腐蚀 mask，取中位深度 |
| 下探撞翻 | 无预抓取或速度过快 | 上方停留 + 慢速下降 |
| 软物仍被捏扁 | 未切入 B2 力控或阈值过高 | 接触后必须进 FORCE_GRASP |
| 放置相撞 | 放置区太近 | config 拉开两区 |
| IK 姿态拧巴 | 低位 + 姿态自由 | 复用 B2 的 grasp pitch / 种子 |
| AI 改爆全局 | vibe coding | 按里程碑与模板约束 |

---

## 10. 和旧文档的关系

| 文档 | 作用 |
|------|------|
| `新手入门讲解.md` | B/B2 是什么、零线代直觉 |
| `README.md` | 安装、已交付 Demo、命令 |
| **本文** | **下一阶段视触融合：怎么做、学什么、架构与里程碑** |
| [`本工程如何用AI开发（提示词说明）.md`](本工程如何用AI开发（提示词说明）.md) | **如何写提示词、如何主导 AI 按里程碑开发（反 vibe coding）** |

---

## 11. 收尾：你完成后应能对外说清的三句话

1. **眼睛**用 RGB-D + 检测把物体放到机械臂基座坐标系。  
2. **手臂**用 IK 粗到预抓取，**手指**用 B2 力控按软硬自适应抓紧。  
3. **任务层**状态机完成分拣；进阶用周期性视觉重跟踪抗移动干扰。

当你能不看稿讲清这三句，并且能指出代码里对应的三个目录（`vision/`、`grasp/`、`main_pipeline.py`）时，你就是在 **AI coding**，而不是 vibe coding。

---

**下一步（仍由你决定）**：先自己在纸上抄一遍第 2 节架构图与第 4.2 节状态机，再开 M1（只挂相机存图）。需要我按本文 **只生成 `task_VTG/` 骨架空文件与 README（仍少写业务逻辑）** 时，再单独下指令即可。
